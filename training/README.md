## Training Assets

- `data/` raw and processed datasets or download scripts
- `notebooks/` experimentation or EDA notebooks
- `scripts/` reproducible preprocessing/training jobs (e.g., `train.py`)
- `models/` exported checkpoints before promotion to `cats_dogs_finetuned_FT.keras`

Keep large artifacts (datasets, model weights) out of git or use git-lfs.

